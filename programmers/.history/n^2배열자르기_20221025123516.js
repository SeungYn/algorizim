'use-strict';

function solution(n, left, right) {
  var answer = [];
  for (let i = 1; i < n + 1; i++) {
    const arr = [].splice(0);
  }
  return answer;
}

console.log(solution(3, 2, 5));
console.log([3] * 3);
const arr = [1, 1, 1, 1];
console.log(arr.fill(4, 0, 3));

console.log(arr);
